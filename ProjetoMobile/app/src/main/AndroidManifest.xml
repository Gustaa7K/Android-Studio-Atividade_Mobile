<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projetomobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectoMobile">

        <!-- Main Activity - Tela Inicial -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Exercicio1Activity -->
        <activity android:name=".Exercicio1Activity" />

        <!-- Exercicio2Activity -->
        <activity android:name=".Exercicio2Activity" />

        <!-- Exercicio3Activity -->
        <activity android:name=".Exercicio3Activity" />

        <!-- Exercicio4Activity - Checkbox para cada letra do nome -->
        <activity android:name=".Exercicio4Activity" />

        <!-- Exercicio5Activity - Preferências de usuário -->
        <activity android:name=".Exercicio5Activity" />

    </application>

</manifest>
